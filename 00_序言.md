## 序　那台跑在別人電腦上的機器

二〇二六年夏天的某個下午三點四十分，一個很普通的問題被打進搜尋框：

> **「請細說 WebAssembly 的發展史、優缺點、競品、著名的應用或是開源專案。」**

那是一個所有工程師都問過的入門問題。可是接下來的一個多小時裡，這條對話沒有停在入門處。它從「Wasm 是什麼」滑到「Wasm 的限制是什麼」，再滑到一個看似無關的岔路——「GitHub Pages 的網頁是靜止的嗎？可以跑 server？」——然後就再也回不去了。

因為那個岔路的盡頭，藏著一個令人不安的事實：**一個一毛錢都不用付的靜態網頁空間，配上一顆 Wasm 引擎，就能跑起 FFmpeg、跑起 Linux、跑起 SQL 資料庫、跑起大型語言模型。** 伺服器不是變便宜了，是被整個拿掉了。運算沒有消失，它只是被推到了每一個打開網頁的人的 CPU 上。

於是那條對話問出了下一個問題：那別人都在上面跑什麼？答案滾了一百二十條。

然後在滾到第一百二十條之後，對話突然轉向，問了一個和技術無關、卻讓整件事重新排列的問題：

> **「在 GitHub Pages 跑程式，不是被看光了？」**

是的，完全被看光了。你的 `.wasm` 檔案會被完整下載到每一台陌生的電腦上。

> **「Figma 若是 Wasm，不是也被看光了？」**

也是的。`figma.wasm` 每天被下載到全球數億台電腦裡。

> **「那不用完整反編譯，只要下載後讓它在本地跑，Figma 不就無法以原本的商業模式進行？」**

這就是這本書真正的起點。**因為答案是「不會」，而理解為什麼「不會」，比理解 Wasm 本身更重要。**

---

### 三種東西，混在同一鍋裡

這趟調查的原始材料，是一份混雜的技術對話紀錄。裡頭浮著三種料，把它們撈開分清，是這本書的第一課：

1. **站得住的事實**：Wasm 的二進位格式與型別系統、線性記憶體的 4GB 定址上限、COOP/COEP 與 `SharedArrayBuffer` 的因果、OPFS 的 `createSyncAccessHandle`、WASI 的能力式安全模型、Figma 與 Photoshop Web 確實靠 Wasm 把 C++ 搬上瀏覽器。這些都可以查證、可以復現。
2. **查不到底的說法**：對話中出現的一百二十個「經典案例」裡，有相當一部分是**真實存在且知名的開源專案**（FFmpeg.wasm、v86、Pyodide、DuckDB-Wasm、SQLite-Wasm、esbuild、Stockfish、OpenCV.js……），但也有相當一部分是**依技術邏輯構造出來的示意案例**（`Web-Biconical`、`Espace3D-Wasm`、`Cubism-OLAP-Wasm`、`Hologram3D-Wasm`……）——它們描述的技術路徑成立，但**專案本身未經查證、極可能不以該名稱存在**。書中一律標「⚠️ 真偽提醒」。所有「快 N 倍」「達原生 X%」的數字同理：**信其量級，疑其小數點。**
3. **會誤導人的直覺**：「二進位很難逆向，所以安全」「被看光就沒有商業模式」「Token 便宜了，所有軟體都能自己造」。這三句話都有一半是對的，而正是那另一半，決定了你的架構會不會在兩年後崩掉。

> ⚠️ 全書的真偽紀律
> 本書橫跨可查證的規範事實與未經查證的社群說法。凡屬後者一律標「⚠️ 真偽提醒」。**規範層面的敘述（二進位格式、規範狀態、瀏覽器 API 行為）請以 WebAssembly 官方規範、MDN 與各引擎的實作文件為準**——本書以 **WebAssembly 3.0**（2025 年 9 月完成）為基準寫成，而這是一個仍在移動的領域。**任何把 GC／memory64／尾呼叫／例外處理稱為「提案」的資料，都寫於 3.0 之前。**

---

### 這趟調查怎麼走

| 篇 | 走到哪 | 對應內容 |
|----|------|------|
| 第一篇　起源與物理 | 這台機器怎麼來的、怎麼運轉（第 1–4 章）| NaCl/asm.js 前史、四巨頭共識、二進位格式與型別系統、分層編譯、線性記憶體、優缺點與限制、與 JS／Docker／V8 Isolates／EVM／**WebGPU／原生**的六場競爭 |
| 第二篇　在靜態頁上造機器 | 把機器搬到一毛錢都不用付的地方（第 5–8 章）| COOP/COEP 那道坎與 `coi-serviceworker`、一百零一個案例的分類全景、MEMFS/IDBFS/OPFS/WASI 儲存四層、4GB 天花板與四種破圈架構 |
| 第三篇　看光之後的護城河 | 機器被下載之後還剩下什麼（第 9–12 章）| 二進位的「天然混淆」與兩大物理禁區、Figma 的四道防線、技術商品化與網絡效應的動態博弈、Token 歸零後的時間陷阱與終局架構 |
| 附錄 | A 大事年表與規範速查 · B 名詞與工具速查 · C GitHub Pages 部署實戰手冊 · **D–F 靜態頁 Wasm 百案圖鑑（101 案逐案深拆，含真偽標記）** · G 儲存機制實作源碼參考 · H 給 AI 編碼代理人的規格書範本 · I 限制與極限速查表 · J 前後端邊界決策表 · K 爭議與真偽校準問答 · **L 深水案例：FluffOS × Wasm——把一整台 MUD 伺服器搬進靜態頁** · **M 規範深水區（十二個常被跳過的技術細節）** · **N 體積與速度（壓縮與加速全解）** · **O 測試、CI 與執行期安全** |

### 三個樂章：懂它、用它、守它

- **第一樂章　懂它（第 1–4 章）**：Wasm 不是「更快的 JavaScript」，它是一台**規格被寫死的抽象機器**——堆疊式虛擬機、結構化控制流、單一連續的線性記憶體、編譯期就能證明安全的型別系統。它快，不是因為它聰明，是因為它**什麼都不讓你動**。而 2015 年為了讓四家瀏覽器坐上談判桌所砍掉的每一樣能力，用了整整十年逐條加回來——**到 2025 年 9 月的 WebAssembly 3.0，那筆債基本還清了。** **目標：知道這台機器的每一條物理定律，因為後面所有的限制都是這些定律的直接推論。**
- **第二樂章　用它（第 5–8 章）**：一台機器最有趣的地方，是它能跑在**不該跑得動它的地方**。GitHub Pages 不給你設 HTTP Header、不給你開連接埠、不給你一行後端程式碼——而社群硬是在上面跑起了影音轉碼、x86 模擬器、OLAP 資料庫與 LLM 推理。**目標：讓機器真的動起來，並且知道它會在哪一格撞牆。**
- **第三樂章　守它（第 9–12 章）**：當你的機器被完整下載到每一台陌生的電腦上，你還剩下什麼？答案不在混淆器裡，在架構圖上。**目標：畫出那條「哪些算力送出去、哪些關係鏈鎖起來」的邊界線——這條線在 LLM 把寫程式的邊際成本壓到接近零之後，是唯一還站得住的東西。**

而如果你想先看一個**同時完成了這三件事的真實例子**——一台把編譯器、虛擬機、telnet 協定全部搬進瀏覽器分頁的 MUD 伺服器，它的全部原始碼三十年來一直可以被完整下載，而它的價值一分都沒有因此消失——**直接翻到附錄 L。** 那是全書核心命題最乾淨的一具活體標本。

懂它、用它、守它——這三個動詞彼此咬合。第一樂章那條 4GB 的線性記憶體上限，直接決定了第二樂章的 FFmpeg.wasm 為什麼吃不下 4K 大檔；而第二樂章「把算力免費推給客戶端」的甜頭，正是第三樂章那道護城河**必須**建在別的地方的原因。

> 💡 **全書的核心命題**
> **能被下載的，遲早會被複製；不能被搬走的，才是護城河。**
> Wasm 把運算變成了可以無限複製、零成本分發的東西——這既是它最大的禮物，也是它對商業模式最徹底的破壞。真正的架構功力，不在於把程式碼藏得多好，而在於**清楚知道哪些東西本來就藏不住，然後把價值放到別的地方去。**

### 關於書名

**組合語言死過一次。**

它曾經是唯一貼著機器的語言，然後被高階語言一層層抽象掉，變成一門只有少數人還在寫、且綁死在某一顆 CPU 上的手藝。**它死於不可攜——一段 x86 組語搬到 ARM 上就是一堆廢位元組。**

而它回來了，只是換了一具身體：**這一次，它的機器不存在。**

WebAssembly 是一台規格上定義出來、物理上並不存在的抽象堆疊機器的組合語言。**正因為那台機器不存在，它才到處都在**——它此刻正在你打開的分頁裡、在幾十億台你永遠不會見到的電腦裡，安靜地運轉，而它們的主人多半不知道。

**一具沒有形體、看不見、卻無所不在的東西，中文有一個現成的詞。**

而這本書真正想講的，是這隻幽靈飄進別人家裡之後的事——**你送出去的那份東西，還算不算你的？**

每章末留一句「💡 君之一席話」當作當晚的筆記；再附一段「🔍 進階點評」，深掘該案背後的工程現實與那條看不見的路。

---

### 怎麼讀這本書

正文十二章是一條線，按順序讀最順。**但十五份附錄不是**——它們是查閱材料，各自對應不同的處境。與其從頭翻，不如照你此刻的問題選一條路：

| 你是誰／你在做什麼 | 建議路徑 |
|---|---|
| **只有十分鐘，想知道這本書在講什麼** | 第 9 章（都被看光了）＋ 附錄 J（前後端邊界決策表）。**這是全書最能直接動用的兩塊。** |
| **要決定「這個功能該不該用 Wasm」** | 第 3 章（限制）→ 第 4 章（競品）→ 附錄 I（極限速查）→ 附錄 J（決策表） |
| **正要把一個 C/C++ 專案搬上網頁** | 第 2 章（物理定律）→ 第 5 章（部署那道坎）→ 第 7 章（儲存）→ 附錄 C（實戰手冊） |
| **模組太大 / 跑太慢** | 第 8 章（天花板）→ **附錄 N（體積與速度全解，含兩張投報率總表）** |
| **要在靜態託管上做持久化** | 第 7 章 → 附錄 G（Rust + OPFS 源碼）。**特別注意第 7 章那個 `opfs-sahpool` 的分岔，它能省下你兩週。** |
| **想知道別人在靜態頁上跑了什麼** | 第 6 章（分類全景與五種通用架構）→ 附錄 D–F（101 案逐案深拆） |
| **要看一個被拆到底的真實案例** | **附錄 L**（FluffOS：一整台 MUD 伺服器跑在瀏覽器分頁裡） |
| **在做架構決策 / 商業判斷** | 第 9–12 章 ＋ 附錄 J |
| **想挖規範層** | 第 2 章 → **附錄 M**（二進位編碼、驗證演算法、`call_indirect`、例外 ABI、JSPI、原子操作……） |
| **要把工作交給 AI 代理人** | 附錄 H（可直接複製的規格書範本） |
| **要建立測試與 CI** | **附錄 O**（三層測試金字塔、`wasm-bindgen-test`、fuzzing、體積與金鑰守門、跨引擎差異） |
| **要做安全評估** | 第 9 章 → 附錄 O §7（執行期本身的攻擊面與供應鏈） |
| **懷疑書裡某句話** | **附錄 K**（爭議與真偽校準問答，把全書所有 ⚠️ 集中起來逐條校準） |

> **如果你只準備讀一份附錄**：想解決眼前的問題就讀 **N**（體積與速度），想搞懂這件事的本質就讀 **L**（那個跑了三十年的開源對照實驗）。

> ⚠️ 教育與研究聲明
> 本書為教育與研究用途，是對相關公開資料與技術現況的整理與評註。書中對二進位保護、逆向工程、程式碼混淆的描述，**僅作為架構決策的風險揭露**，不構成規避他人授權、破解商業軟體或繞過技術保護措施的操作建議。分析你**有權分析**的二進位是正當行為；對他人受保護的商業軟體進行破解與再分發，可能違反授權契約、著作權法與相關技術保護措施條款。

咖啡涼了一半，搜尋框裡的游標還在閃。第一個問題其實很小：**這東西到底是從哪來的？** 我們從一個失敗的 Google 專案開始講起。
