# 第2章　二進位的物理學：魔數之後的每一個位元組

> 打開任何一個 `.wasm` 檔案，開頭永遠是這八個位元組：`00 61 73 6D 01 00 00 00`。前四個是魔數 `\0asm`，後四個是版本號 `1`。**這八個位元組之後的一切，都是為了讓一台不信任你的機器，能在讀完之前就證明你不會作惡。**

## 情境 1　它不是機器碼，是一份可以被證明的合約

**背景**：多數人對 Wasm 的第一印象是「編譯後的機器碼」。這個印象是錯的，而且錯得很關鍵。**Wasm 不是任何一顆實體 CPU 的指令集**——沒有 x86，沒有 ARM，沒有 RISC-V。它是一台**規格上定義出來的抽象堆疊機器**，而這台機器的每一條性質，都是為了「可驗證性」服務的。

**模組的物理結構**：一個 `.wasm` 檔案由若干個**區段（section）**線性排列而成，每個區段以一個位元組的 ID 開頭，接著是 LEB128 編碼的長度，然後是內容。順序是規範強制的：

| ID | 區段 | 內容 |
|---|---|---|
| 1 | Type | 所有函數簽章（`(i32, i32) -> i32` 這種） |
| 2 | Import | 從宿主環境要進來的東西：函數、記憶體、表、全域變數 |
| 3 | Function | 每個函數對應到 Type 區段的哪一個簽章 |
| 4 | Table | 函數參考表（間接呼叫的跳轉目標） |
| 5 | Memory | 線性記憶體的初始頁數與上限 |
| 6 | Global | 全域變數 |
| 7 | Export | 對外曝露的函數/記憶體/表/全域 |
| 8 | Start | 實例化後自動執行的函數 |
| 9 | Element | 表的初始內容 |
| 10 | Code | 每個函數的實際指令與區域變數 |
| 11 | Data | 線性記憶體的初始資料 |
| 12 | DataCount | 資料段數量（bulk memory 提案引入，供單趟驗證用） |
| 0 | Custom | 任何自訂資料（除錯符號 `name`、Source Map 連結、語言專屬中繼資料） |

**這個順序不是美學，是工程**：型別先於函數宣告，函數宣告先於函數本體。這保證了驗證器**只需要從頭到尾掃一遍**——每讀到一條指令，它需要的所有型別資訊都已經在前面出現過了。這就是為什麼 Wasm 的驗證是 **O(n) 單趟線性時間**，也是為什麼它能**邊下載邊編譯**。

**指令是怎麼運作的**：Wasm 是堆疊機器。`local.get 0` 把第 0 號區域變數推上堆疊，`i32.add` 彈出兩個值、推回一個。一個把兩數相加的函數，在文字格式（WAT）裡長這樣：

```wat
(module
  (func $add (param $a i32) (param $b i32) (result i32)
    local.get $a        ;; 堆疊: [a]
    local.get $b        ;; 堆疊: [a, b]
    i32.add)            ;; 堆疊: [a+b]
  (export "add" (func $add)))
```

對應的指令序列是 `20 00 20 01 6A`——五個位元組。`0x20` 是 `local.get`，`0x6A` 是 `i32.add`。**沒有暫存器分配、沒有定址模式、沒有指令前綴**，因此它極度緊湊，也極度容易被機械地翻譯成任何真實 CPU 的指令。

> **精確一點說**：這五個位元組是**指令**，而 Code 區段裡完整的「函數本體」還要在前面加一個區域變數宣告（此例為 `00`，代表零組區域變數），並在結尾加上 `0x0B`（`end`）——**這兩個位元組是規範強制的**。整個模組逐位元組的完整拆解（含 LEB128 編碼、型別編碼表、常用指令碼速查）見**附錄 M 第一節**。

**驗證器在載入時到底檢查什麼**：這是 Wasm 安全模型的核心，它檢查的東西比大多數人以為的多：

1. **堆疊型別一致性**：每條指令執行前後，運算元堆疊的型別序列必須可被靜態推導。`i32.add` 之前堆疊頂端**必須**是兩個 `i32`，不能是 `f64`，也不能是空的。
2. **控制流結構化**：Wasm **沒有任意跳轉（goto）**。控制流只有 `block` / `loop` / `if` 三種區塊，以及 `br` / `br_if` / `br_table` 三種**只能往外跳到某個包住自己的區塊標籤**的分支。這保證了控制流圖天生是可歸約的（reducible），編譯器不需要做昂貴的不可歸約迴圈分析。
3. **分支處的堆疊高度必須一致**：跳到同一個標籤的所有路徑，堆疊上的型別必須相同。
4. **區域變數索引在界內**、**函數索引在界內**、**間接呼叫的簽章在執行期比對**（`call_indirect` 的四道檢查與 C++ 虛擬函數的實作方式見附錄 M 第三節）。
5. **記憶體與表的存取一律有界檢查**（下一節詳談）。

**還有一個很聰明的技巧值得單獨提**：`unreachable` 之後的死程式碼怎麼驗證？規範用「**多型堆疊（polymorphic stack）**」——進入 unreachable 狀態後，驗證器把堆疊視為「可提供任意數量、任意型別的值」，於是任何死程式碼都能通過驗證，**而驗證器不必做可達性分析**。**這個看起來像特例的規則，正是為了保住「單趟 O(n)」這個更根本的性質**（詳見附錄 M 第二節）。

**通過驗證的模組有一個極強的性質**：它**不可能**讀寫線性記憶體之外的任何位元組、不可能跳到任意位址、不可能偽造函數指標、不可能破壞呼叫堆疊（return address 存在引擎自己的堆疊上，Wasm 程式碼碰不到）。**緩衝區溢位攻擊在 Wasm 裡打不穿沙盒**——它最多只能弄壞這個模組自己線性記憶體裡的資料。

> ⚠️ 真偽提醒（一個廣為流傳的誤解）
> 「Wasm 記憶體安全」這句話**只在沙盒邊界的意義上成立**。它保證的是「Wasm 模組傷不到宿主」，**不保證你用 C 寫的程式在 Wasm 裡就沒有記憶體錯誤**。事實上情況更微妙：因為線性記憶體是一整塊平坦的 `ArrayBuffer`，**傳統的堆疊保護（stack canary）、位址空間隨機化（ASLR）、不可執行頁（NX）這些原生平台的緩解措施在 Wasm 裡多半不存在**。一個在 x86 上會被 ASLR 擋下的緩衝區溢位，在 Wasm 裡可能穩定可利用——只是它的破壞範圍被限制在該模組的線性記憶體內。學術界對此有明確的討論（如 "Everything Old is New Again: Binary Security of WebAssembly"，USENIX Security 2020）。**沙盒安全 ≠ 應用安全。**

> 💡 君之一席話
> 與君一席話：**安全性最便宜的實現方式，不是加防護，是砍能力。** Wasm 沒有 `goto`、沒有暫存器、沒有裸指標、沒有可執行的堆疊、沒有系統呼叫——它不是「守住」了這些攻擊面，是**這些攻擊面在規格層面根本不存在**。守衛是要花錢的（執行期檢查、CPU 週期、複雜度、以及每一個守衛自己可能的漏洞）；不存在是免費的，而且不會有 bug。這條原理在系統設計裡有一個更常見的名字：**讓非法狀態無法被表示（make illegal states unrepresentable）。** 每當你在做安全設計時發現自己在寫「檢查使用者有沒有做 X」，先退一步問一句：**能不能讓 X 在型別、在協定、在資料結構上根本無法被寫出來？** 能砍掉的，永遠比能守住的可靠。

> 🔍 進階點評──結構化控制流的代價，以及那個沒人提的編譯難題
> 「沒有 goto」聽起來只是安全設計，但它對編譯器作者是一場災難，而這場災難的解法很有意思。**問題在於：真實世界的程式碼有 goto。** C 有 `goto`，LLVM IR 的控制流圖是任意的有向圖，而 Wasm 只接受由巢狀區塊組成的樹狀結構。把任意 CFG 還原成結構化控制流，是編譯理論裡的老問題（Relooper 演算法），Emscripten 早期就是靠它，後來 Binaryen 加入了更強的 **Stackify** 演算法。代價是什麼？**對於某些不可歸約的迴圈（irreducible loops），你必須引入一個額外的狀態變數與一個大 `br_table`，把控制流「模擬」出來**——這會產生比原始程式碼更慢的指令序列。這就是為什麼某些手寫組語密集的專案（如影片解碼器）移植到 Wasm 後，效能損失比預期大：**不是 Wasm 指令慢，是它的控制流表達能力不夠，逼編譯器繞路。** 後來的 **funclets / multi-loop 提案**討論過放寬這一點，但至今核心規範仍守著結構化控制流不放——因為那是驗證器能在單趟線性時間內完成的前提。**這是一個非常典型的取捨案例：為了讓每一台裝置上的驗證都便宜到可以忽略不計，規範願意犧牲一小撮極端場景的效能。** 當你評估任何一個「安全的執行環境」時，都應該去找這個取捨點在哪——它一定存在，而且它一定會在某個角落咬你。

## 情境 2　線性記憶體：那塊平坦的、只能長不能縮的荒原

**背景**：Wasm 的記憶體模型簡單到近乎原始——**一整塊連續的、可定址的位元組陣列**，稱為線性記憶體（linear memory）。它以 **64KiB 為一頁**，用 `memory.grow` 指令按頁增長，用 `memory.size` 查詢當前頁數。

**在 JavaScript 側，它就是一個 `ArrayBuffer`**：

```javascript
const { instance } = await WebAssembly.instantiateStreaming(fetch("app.wasm"), imports);
const mem = instance.exports.memory;                 // WebAssembly.Memory
const bytes = new Uint8Array(mem.buffer);            // 直接看到 Wasm 的整塊記憶體
const ints  = new Int32Array(mem.buffer);            // 同一塊記憶體的另一種視角
```

**這一點是所有 Wasm 效能技巧的根**：JS 與 Wasm 之間**不需要複製資料**，因為它們看的是同一塊位元組。所謂「零拷貝」，指的就是用 `TypedArray` 在這塊 buffer 上開一個視圖，而不是把資料搬來搬去。

**它的四條物理性質，逐條都會在後面的章節咬人**：

1. **只能增長，不能縮小**。`memory.grow` 有，`memory.shrink` 沒有。一個曾經吃到 3GB 的 Wasm 應用，即使釋放了所有內部資料結構，**那 3GB 的 `ArrayBuffer` 依然被作業系統記在帳上**，直到整個實例被丟棄。這就是為什麼長時間執行的 Wasm 應用要嘛設計成「用完就丟掉整個實例」，要嘛在內部自建記憶體池。
2. **增長可能導致 `ArrayBuffer` detach**。在不使用 `growable ArrayBuffer` 的舊行為下，`memory.grow` 之後**所有既有的 `TypedArray` 視圖都會失效**（buffer 被 detach）。這是新手最常踩的雷：

```javascript
let view = new Uint8Array(mem.buffer);
instance.exports.allocate_lots();   // 內部觸發了 memory.grow
view[0] = 42;                       // ❌ TypeError / 寫進了已 detach 的 buffer
// 正解：每次可能觸發 grow 的呼叫之後，重新取得視圖
view = new Uint8Array(mem.buffer);
```

3. **位址就是索引，`0` 是合法位址**。Wasm 沒有「空指標」的概念——位址 0 是線性記憶體的第一個位元組，完全可讀可寫。所以 C 語言裡「解參考 NULL 會 segfault」的保護在 Wasm 裡**默認不存在**（Emscripten 可以用 `--low-memory-unused` 之類的選項把低位址保留起來當作陷阱區，但那是工具鏈的補丁，不是規範的保證）。
4. **越界存取一定會 trap**。這是規範的硬保證。`i32.load` 一個超出當前記憶體大小的位址，會產生一個**不可捕捉的陷阱（trap）**，在 JS 側表現為拋出 `WebAssembly.RuntimeError`。

**界檢查怎麼做到不慢**：天真的實作會在每一次 `load`/`store` 前插入一條比較與分支，那是不可接受的開銷。現代引擎在 64 位元宿主上用一招漂亮的技巧——**保護頁（guard page）**：

> 引擎在虛擬位址空間裡為每個 32 位元線性記憶體保留 **8GiB**（4GiB 的可定址空間，加上前後的保護區），但只把實際用到的頁面**提交**（commit）為可讀寫。位址計算用 32 位元的偏移量做，因此**在數學上不可能超出那 4GiB 的保留區**；一旦存取到尚未提交的區域，作業系統的分頁機制會產生 SIGSEGV，引擎的訊號處理器接住它，翻譯成 Wasm trap。
> **結果：熱路徑上的界檢查指令數是零，代價由 MMU 硬體免費承擔。**

這也解釋了一件事：為什麼 Wasm 在 32 位元宿主或某些嵌入式執行期上會明顯變慢——**那裡沒有足夠的虛擬位址空間做保留，只能退回逐次顯式比較。**

> 💡 君之一席話
> 與君一席話：**最高明的效能優化，通常長得不像優化，而像是把工作交給了某個本來就免費的東西。** 保護頁這一招沒有「加速」任何運算，它只是把「檢查位址是否越界」這件事，從軟體搬到了 MMU 這個**本來就在每一次記憶體存取時都會運轉的硬體**上——換句話說，它讓界檢查的邊際成本變成零，因為那個成本已經有人在付了。這條思路在系統工程裡到處都是：mmap 讓檔案讀取搭上分頁的便車，copy-on-write 讓 fork 搭上 MMU 的便車，DMA 讓 I/O 搭上匯流排的便車。**當你為某件事的開銷苦惱時，最有價值的問題不是「怎麼讓它更快」，而是「這件事有沒有可能是某個已經在跑的機制的副產品」。**

> 🔍 進階點評──4GB 這個數字的三段式演化，與它真正的攔路虎
> 線性記憶體的 4GiB 上限來自 `i32` 位址（2³² 位元組）。這個數字在實務上有三層天花板，而**它們互相遮擋**，導致很多人搞不清楚自己是撞到哪一層。**第一層，規範層：wasm32 死鎖在 4GiB。** 這是數學，繞不過。**第二層，引擎層：瀏覽器通常撐不到 4GiB。** V8 對單一 Wasm 記憶體有自己的上限（歷來在 2GiB~4GiB 之間變動且與平台有關），實務上很多場景在 2~3GiB 就先 OOM 了。**第三層，分頁層：`ArrayBuffer` 需要連續的虛擬位址空間。** 在 32 位元瀏覽器或位址空間碎片化嚴重的長時間執行分頁上，即使理論額度還有，`memory.grow` 也可能單純因為找不到一塊夠大的連續區間而失敗。**破局的是 Memory64（`i64` 定址，已於 Wasm 3.0 進入核心規範）**，理論上限提到 2⁶⁴，但這裡有個常被忽略的代價：**一旦位址變成 64 位元，前面那套「保留 8GiB 虛擬空間 + 保護頁」的免費界檢查就不成立了**——你不可能為每塊記憶體保留 2⁶⁴ 的虛擬位址。因此 memory64 在多數實作上**必須退回顯式界檢查**，帶來可觀的效能回退（不同基準下數個百分點到雙位數不等）。所以正確的心智模型不是「memory64 出來就沒問題了」，而是——**你在 4GiB 以下享受的是硬體補貼的免費安全，越過那條線之後，安全就要開始收費了。** 這對架構決策的意義很直接：**能靠分塊串流或多重記憶體（第 8 章）在 4GiB 以內解決的，就別急著跳 Memory64。**

## 情境 3　從二進位到機器碼：分層編譯與那條看不見的預熱曲線

**背景**：使用者按下 Enter 之後，那個 20MB 的 `.wasm` 到底經歷了什麼？答案不是「解析完就開始跑」，而是一條**同時在賽跑的雙軌流水線**。

**現代引擎（以 V8 為例）的分層策略**：

| 層 | 編譯器 | 特性 | 用途 |
|---|---|---|---|
| Tier 1 | **Liftoff** | 單趟、不做優化、幾乎是逐條指令翻譯 | **極快產出可執行碼**（數百 MB/s 級的編譯吞吐），讓程式立刻能跑 |
| Tier 2 | **TurboFan** | 完整的 SSA 中間表示、內聯、迴圈優化、暫存器分配 | 產出高品質機器碼，**在背景執行緒編譯**，好了就熱替換 |

SpiderMonkey 有對應的 **BaseLine → Ion** 雙層，JavaScriptCore 則有 **BBQ → OMG**。**三家引擎不約而同選擇了同一個形狀，因為問題是同一個**：使用者要的是「立刻開始跑」，長期效能要的是「編譯得夠好」，而這兩件事無法同時滿足——除非你做兩次。

**串流編譯（streaming compilation）**：這是 Wasm 相對 JS 最被低估的優勢。

```javascript
// ✅ 正確：一邊下載一邊編譯，第一個位元組到達時 Liftoff 就開工了
const { instance } = await WebAssembly.instantiateStreaming(
  fetch("app.wasm"), importObject);

// ❌ 常見錯誤：先把整包下載成 ArrayBuffer，再開始編譯（白白多等一整趟下載）
const buf = await (await fetch("app.wasm")).arrayBuffer();
const { instance } = await WebAssembly.instantiate(buf, importObject);
```

`instantiateStreaming` 要求伺服器回傳的 `Content-Type` 必須是 `application/wasm`，否則會拒絕。**GitHub Pages 對 `.wasm` 副檔名會正確回傳這個 MIME**，這是它意外地適合跑 Wasm 的原因之一。

**程式碼快取（code caching）**：Chrome 會把 TurboFan 編譯的結果寫進磁碟快取，第二次造訪同一個 URL 時可直接載入編譯結果，跳過整個編譯階段。這使得**大型 Wasm 應用的第二次載入往往比第一次快一個量級**——也是為什麼那些「Pyodide 要下載 30MB」的抱怨，在回訪使用者身上感受完全不同。

**冷啟動的完整成本拆解**（這是評估「值不值得用 Wasm」時真正該算的帳）：

```
總冷啟動時間 =
    網路傳輸（受 gzip/brotli 壓縮後體積與 RTT 支配）
  + Liftoff 編譯（≈ 與位元組數線性相關，通常遠小於網路時間）
  + 實例化（配置線性記憶體、跑 Data 段初始化、執行 start 函數）
  + 執行期初始化（C++ 的全域建構子、Rust 的 lazy static、語言 runtime 的自舉）
  ─────────────────────────────────────────────
  ⚠️ 最後一項是最常被低估的：Pyodide 的 CPython 自舉、
     Emscripten 的檔案系統掛載，都發生在「編譯完成之後」。
```

**優化順序也因此被決定**：先砍體積（`wasm-opt -Oz`、`--strip-debug`、按需分割模組），再砍執行期初始化（延遲初始化、快照），最後才輪到指令層優化。**把力氣花在指令層而不管那 30MB 的下載，是最常見的誤配。**

**關於第四段，有一項專門的武器值得先記住名字**：**Wizer**（Bytecode Alliance）可以在**建置時**就把初始化跑完，並把「初始化之後的記憶體狀態」快照回一個新的 `.wasm`——執行期拿到的模組，Data 區段裡已經是初始化完成的映像。官方基準宣稱**實例化與初始化快 1.35 到 6.00 倍**。**代價是 Data 區段變大，也就是拿體積換啟動速度**——這正是本節那條「四段成本各自獨立」的直接推論。

> **體積與速度是一整套互相牽制的技術，值得單獨拆解。** 從「你的位元組花在哪裡」的解剖方法，到 `wasm-opt` 每個 pass 真正做什麼、Rust 那個藏在 `panic!` 裡的格式化機制、壓縮字典傳輸如何讓改版只傳幾十 KB、Wizer 與後端 AOT、SIMD 自動向量化的三個現實、以及一個會讓你量錯數字的計時器精度陷阱——**全部收在附錄 N。**

> 💡 君之一席話
> 與君一席話：**當一個系統必須同時滿足兩個互相矛盾的指標時，正確的答案往往不是折衷，而是分層。** Liftoff 與 TurboFan 的關係不是「取中間值」——那樣會得到一個既不夠快啟動、也不夠快執行的平庸編譯器。它是**先用一個爛到極致但快到極致的方案讓系統活起來，再在背景默默把它換成好的**。這個模式的名字叫**漸進式優化（progressive optimization）**，而它出現在所有你想得到的地方：圖片的漸進式載入、資料庫的 LSM tree（先寫快的、背景合併成好的）、CDN 的 stale-while-revalidate、甚至 CPU 的分支預測（先猜一個，錯了再回滾）。**每當你在「快速回應」與「高品質結果」之間糾結時，先問一句：這兩者能不能不要在同一個時間軸上競爭？**

> 🔍 進階點評──為什麼「Wasm 有 60~80% 的原生效能」這句話幾乎沒有意義
> 這是本書必須早點戳破的一句話，因為它會在附錄的一百零一個案例裡出現一百零一次。**這個數字的成立高度依賴於工作負載的形狀。** 拆開來看：**（一）純整數與浮點運算密集、資料放得下 L1/L2 快取、控制流簡單的迴圈**——Wasm 可以逼近原生的 90%+，因為 TurboFan 產出的機器碼和 Clang -O2 差不了多少。**（二）需要手寫 SIMD 或平台特化組語的場景**——Wasm SIMD 只有 128 位元固定寬度（`v128`），而現代 x86 有 AVX2（256 位元）與 AVX-512，ARM 有 SVE。一個為 AVX2 手寫優化的影片解碼器移植到 Wasm，**掉到原生的 40~50% 是常態**，因為它的向量寬度直接減半，而且 relaxed SIMD 提案雖然放寬了部分語意以換取更好的映射，仍補不回寬度差。**（三）大量跨越 JS/Wasm 邊界的細碎呼叫**——每次跨界都有固定開銷，如果你的函數本體只做幾條指令，那開銷會佔絕大部分，此時 Wasm 可能比純 JS **還慢**。**（四）大量字串與物件操作**——Wasm 沒有字串型別，每一次字串往返都是編碼、複製、解碼三步，純 JS 在這裡是碾壓性的。**（五）記憶體存取模式差、快取未命中主導**——此時瓶頸在記憶體匯流排，Wasm 與原生的差距會收斂到接近零（因為兩邊都在等 DRAM）。所以正確的說法不是「Wasm 有 N% 的原生效能」，而是——**Wasm 消除了 JavaScript 的動態型別檢查、垃圾回收暫停與物件分配開銷，剩下的差距取決於你的工作負載有多依賴那些 Wasm 規格裡沒有的東西。** 讀任何 Wasm 效能宣稱時，第一個要問的問題永遠是：**這個基準測試的形狀，跟我要跑的東西像不像？**

## 情境 4　跨越邊界：`wasm-bindgen`、膠水程式碼，與那條真正的收費站

**背景**：Wasm 只認得四種數值型別（`i32`、`i64`、`f32`、`f64`，加上 SIMD 的 `v128` 與參考型別）。**它不認得字串、不認得陣列、不認得物件、不認得 `null`。** 那 `wasm-pack` 產出的 JS 裡，`greet("GitHub Pages")` 是怎麼跑起來的？

**答案是：那不是一次呼叫，是一整套協定。** 以 Rust + `wasm-bindgen` 為例，一次傳字串的完整流程：

```rust
#[wasm_bindgen]
pub fn greet(name: &str) -> String {
    format!("Hello, {}!", name)
}
```

底下實際發生的事：

```
JS 側                                    Wasm 側
─────────────────────────────────────────────────────────
1. TextEncoder 把 "GitHub Pages"
   編碼成 UTF-8 位元組
2. 呼叫 wasm.__wbindgen_malloc(len)  →   分配線性記憶體，回傳位址 ptr
3. 把位元組寫進 memory.buffer 的
   ptr 位置（一次記憶體複製）
4. 呼叫 wasm.greet(ptr, len)        →   Rust 用 ptr/len 建構 &str
                                         （這一步零拷貝，直接指向線性記憶體）
                                         執行 format!，結果寫進新分配的區塊
                                    ←    透過回傳區把 (ptr2, len2) 傳回
5. 從 memory.buffer 的 ptr2 讀出位元組
6. TextDecoder 解碼成 JS 字串（又一次複製）
7. 呼叫 wasm.__wbindgen_free(ptr2)  →   釋放
```

**一次「傳個字串」，付出了兩次編解碼、至少兩次記憶體複製、三次跨界呼叫。** 這就是為什麼所有 Wasm 效能建議裡最重要的一條是：**把邊界做粗，不要做細。**

**具體的優化手法**：

- **批次化**：與其呼叫 `process_pixel()` 一百萬次，不如呼叫 `process_image(ptr, width, height)` 一次。
- **視圖而非複製**：Rust 側用 `js_sys::Uint8Array::view(&data)` 直接把切片映射成 JS 視圖（注意這是 `unsafe`，因為只要 Wasm 側發生 `memory.grow`，這個視圖就失效了）。
- **共享緩衝區**：由 JS 分配一塊大 buffer，雙方約定用偏移量溝通，整個處理過程不再跨界。
- **避免每幀回呼**：物理引擎、遊戲迴圈千萬不要每個物件一次回呼；改用「Wasm 寫進一塊結果緩衝區，JS 每幀讀一次」的環形佇列模式。

**兩種主要工具鏈的分工**：

| | Emscripten | `wasm-bindgen` / `wasm-pack` |
|---|---|---|
| 語言 | C / C++（也支援其他 LLVM 前端） | Rust |
| 產出 | `.wasm` + 一大包 JS 膠水（含 libc、檔案系統、SDL/OpenGL 轉譯層） | `.wasm` + 精簡 JS 綁定（只含你用到的型別轉換） |
| 哲學 | **模擬一整個 POSIX 環境**，讓既有 C 程式幾乎不改就能編 | **只做型別橋接**，其他交給 Web API |
| 適合 | 移植現成的大型 C/C++ 專案（FFmpeg、SQLite、OpenCV） | 從零寫的新專案、對體積敏感的函式庫 |
| 典型體積負擔 | 較大（含 runtime 與 shim） | 較小（幾 KB 到數十 KB 的膠水） |

**Emscripten 的膠水到底在做什麼**：它遠不只是型別轉換。它實作了一整套 **MEMFS**（記憶體虛擬檔案系統，讓 `fopen("video.mp4","rb")` 這種呼叫有地方可去）、把 SDL2 的繪圖指令轉譯成 WebGL、把 `pthread` 映射到 Web Worker + `SharedArrayBuffer`、把 `printf` 接到 `console.log`。**這就是為什麼 OpenTTD、VLC 這種數十萬行的 C++ 遊戲與播放器可以「幾乎不改核心邏輯」就搬上網頁**——不是因為 Wasm 神奇，是因為 Emscripten 在下面墊了一整層假的作業系統。

> 💡 君之一席話
> 與君一席話：**任何兩個異質系統之間的介面，成本都不在「傳輸」，而在「翻譯」。** JS 與 Wasm 之間傳一個 `i32` 幾乎免費，傳一個字串卻要付兩次編解碼與兩次複製——差別不在資料量，在於**兩邊對「什麼是一個值」的定義不同**。這條規律的適用範圍遠遠超出 Wasm：ORM 的物件關聯阻抗不匹配、微服務之間的 JSON 序列化、Python 與 C 擴充之間的 GIL 邊界、GPU 與 CPU 之間的資料上傳——**每一個都是同一個病，而每一個的解法也都一樣：把介面做粗、把往返做少、讓資料在同一側待久一點。** 當你發現自己在優化一個介面的傳輸速度時，先確認你不是在優化一件本來就不該做那麼多次的事。

## 本章小結

- Wasm **不是機器碼，是一台抽象堆疊機器的指令編碼**。區段順序、結構化控制流、堆疊型別一致性——每一條設計都是為了讓驗證能在 **O(n) 單趟線性時間**完成，這才是「邊下載邊編譯」的前提。
- 通過驗證的模組**不可能**越界讀寫、任意跳轉或破壞呼叫堆疊。但**沙盒安全 ≠ 應用安全**：線性記憶體內部沒有 ASLR/NX/stack canary，C 程式的記憶體錯誤在 Wasm 裡反而可能更穩定可利用（見情境 1 ⚠️）。
- **線性記憶體只能長不能縮**，`memory.grow` 會使既有的 `TypedArray` 視圖失效，位址 0 合法可寫，越界必 trap。界檢查靠**保留 8GiB 虛擬位址 + 保護頁**做到熱路徑零指令——代價由 MMU 免費承擔。
- 驗證器用**多型堆疊**處理 `unreachable` 之後的死程式碼——一個看起來像特例的規則，實際上是為了保住「單趟 O(n)」這個更根本的性質（附錄 M §2）。
- **分層編譯（Liftoff → TurboFan）** 讓「立刻能跑」與「跑得夠快」不必在同一個時間軸上競爭；`instantiateStreaming` + 正確的 `application/wasm` MIME 是免費的啟動優化，程式碼快取讓回訪成本降一個量級。
- **啟動是四段獨立的成本**（傳輸／編譯／實例化／執行期初始化），而**第四段最常被低估**。針對它有專門的武器——**Wizer** 可在建置時跑完初始化並把記憶體狀態快照回模組（附錄 N §10-2）。
- 「Wasm 有 60~80% 原生效能」是一句**幾乎沒有資訊量的話**——SIMD 寬度、跨界頻率、字串比重、快取行為，每一項都能把這個數字挪動一倍以上（見情境 3 🔍）。
- **邊界是收費站**：一次字串往返 = 兩次編解碼 + 兩次複製 + 三次跨界呼叫。**把介面做粗、把往返做少**，是所有 Wasm 效能優化裡回報最高的一條。

物理定律清點完畢。下一章把這些定律翻譯成人話——**哪些事 Wasm 做得漂亮，哪些事它做不到，以及哪些事它做得到但你不該讓它做。** 翻到第 3 章。
