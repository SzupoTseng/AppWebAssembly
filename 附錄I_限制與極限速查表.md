# 附錄I　限制與極限速查表

> 一頁看完所有會撞到的牆。**每一條都標明「這是規範限制、引擎限制，還是實務建議」**——因為它們的可繞過性完全不同。

---

## 一、大小與記憶體

| 項目 | 數值 | 性質 | 繞法 |
|---|---|---|---|
| 線性記憶體頁大小 | **64 KiB** | 規範 | — |
| wasm32 定址上限 | **4 GiB**（2³²） | **規範，繞不過** | 串流分塊；多實例隔離；**多重記憶體**；Memory64 |
| 瀏覽器實際記憶體上限 | 常在 **2–3 GiB** 就 OOM | 引擎（隨版本/平台變動） | 降低初始記憶體、分塊處理 |
| wasm64 定址上限 | 2⁶⁴（理論） | 規範 | **代價：失去保護頁的免費界檢查** |
| `.wasm` 檔案大小（瀏覽器） | 硬性 1–2 GB 級 | 引擎 | — |
| `.wasm` 檔案大小（實務建議） | **10–30 MB 以內** | **實務建議** | **先砍資料**、`wasm-opt -Oz`、模組分割、Brotli |
| 真實參照（FluffOS 驅動器） | **3.6 MB 原始 / 0.8 MB Brotli**；ICU 資料 30MB→780KB | 實測 | 見附錄 L |
| `.wasm` 檔案大小（後端） | 無明確上限 | — | 受實體記憶體支配 |
| `memory.grow` 方向 | **只能增長，不能縮小** | 規範 | 用完丟掉整個實例（微秒級） |
| 位址 0 | **合法可讀寫**（無空指標保護） | 規範 | Emscripten 可保留低位址當陷阱區 |

---

## 二、執行緒與並行

| 項目 | 限制 | 性質 |
|---|---|---|
| Wasm 原生建立執行緒 | **不能**。必須靠宿主（Web Worker / OS thread） | 規範 |
| 共享記憶體 | 需要 `SharedArrayBuffer` + 多執行緒特性 | 規範 |
| `SharedArrayBuffer` 前提 | **跨來源隔離**（COOP + COEP 兩個 HTTP Header） | 瀏覽器安全策略 |
| GitHub Pages 設 Header | **不允許** | 平台限制 |
| 繞法 | `coi-serviceworker`（前端合成 Header）；或改用多實例隔離 | — |
| 多實例隔離的記憶體 | **4 GiB × N**（各自獨立） | — |
| 多實例隔離是否需要隔離 | **❌ 不需要** | — |

---

## 三、SIMD

| 項目 | 數值 | 對照 |
|---|---|---|
| Wasm SIMD 向量寬度 | **固定 128 位元（`v128`）** | x86 AVX2 = 256、AVX-512 = 512；ARM SVE 可變 |
| 典型加速比 | **2–4 倍**（不是 4–16 倍） | 記憶體頻寬與資料重排會吃掉一部分 |
| 是否需要偵測 | **是**，老瀏覽器沒有 | 需準備非 SIMD 備援建置 |
| Relaxed SIMD | 放寬語意換效能，**結果可能因平台而異** | 鏈上/需確定性的場景必須禁用 |

---

## 四、跨界（JS ↔ Wasm）

| 項目 | 成本 |
|---|---|
| 傳一個 `i32` | 幾乎免費（奈秒級） |
| 傳一個字串（雙向） | **兩次編解碼 + 兩次複製 + 三次跨界呼叫**（可到微秒級） |
| `postMessage` 結構化複製 | O(n) |
| `postMessage` + Transferable | **O(1)**（轉移所有權，來源側立刻失效） |
| `SharedArrayBuffer` | O(1)（雙方同時可見，需隔離） |
| 直接操作 DOM | **不能**，必須經 JS |
| **黃金法則** | **把邊界做粗、把往返做少** |
| 同步程式碼等非同步 API | **JSPI**（Chrome 137+／FF 139+）掛起整條 Wasm 堆疊；退路是 Asyncify（貴） |

---

## 五、儲存

| 機制 | 持久 | 隨機存取 | 吃線性記憶體 | 主執行緒可用 | 適用 |
|---|---|---|---|---|---|
| **MEMFS** | ❌ | ✅（在記憶體裡） | **✅ 致命** | ✅ | 暫存中間檔 |
| **IDBFS** | ✅ | ❌（整包倒帶） | 同步時吃 | ✅ | 遊戲存檔、設定（<幾 MB） |
| **OPFS（async）** | ✅ | 有限 | ❌ | ✅ | 一般檔案 |
| **OPFS（sync handle）** | ✅ | **✅ 真 pread/pwrite** | ❌ | **❌ 只能 Worker** | 資料庫、大檔案、串流 |
| **WASI（後端）** | ✅ | ✅ | ❌ | — | 伺服器端 |

**額外限制**：OPFS 同一檔案預設只能有**一個** sync access handle（獨占鎖；`{mode:"readwrite-unsafe"}` 可開多個，但並行控制責任歸你）；未 `persist()` 的資料可能在磁碟壓力下被驅逐；使用者看不見 OPFS，必須自行提供匯出功能。

---

## 六、語言與執行期負擔

| 語言 | 典型 Hello World 體積 | 是否適合 |
|---|---|---|
| Rust / C / C++ / Zig | 數 KB – 數十 KB | **★ 天生一對** |
| AssemblyScript | 數 KB | ★ 前端平滑入口 |
| Go（標準工具鏈） | ~1.5 MB+ | ⚠️ 體積是主要痛點（TinyGo 可大幅縮減） |
| C# / Blazor | 數 MB 級 | ⚠️ 生態強，冷啟動與體積是代價 |
| Java / Kotlin | 傳統上極大 | **Wasm GC 落地後才真正可用** |
| Python (Pyodide) | 30–50 MB 全家桶 | 只在「需要整個科學生態」時划算 |

**執行效能的兩層現實**（以 Pyodide 為例）：純 Python 迴圈約原生的 **1/3–1/5**；呼叫 NumPy 的 C 算子約 **70%**。**在虛擬機裡跑解譯器，兩層抽象疊加。**

---

## 七、規範層面「不存在」的東西

| 沒有 | 後果 | 對應提案 |
|---|---|---|
| `goto` / 任意跳轉 | 編譯器必須把 CFG 結構化（Relooper/Stackify），少數不可歸約迴圈會慢 | multi-loop（未落地） |
| 字串型別 | 每次往返要編解碼 | JS String Builtins |
| 物件 / 垃圾回收（MVP 沒有） | 高階語言要自帶 GC → 體積暴增 | **✅ 已進 Wasm 3.0** |
| 例外處理（MVP 沒有） | C++ try/catch 靠 JS 蹦床 | **✅ 已進 Wasm 3.0**（Tag 區段 + `try_table`/`exnref`） |
| 尾呼叫（MVP 沒有） | 函數式語言深層遞迴爆棧 | **✅ 已進 Wasm 3.0**（`return_call`） |
| 阻塞／等待非同步 | 同步 C 程式碼無法等 Promise | **JSPI**（2025-04 標準化，Chrome 137+／FF 139+） |
| 系統呼叫 | 一切能力必須由宿主 import | **WASI** |
| ASLR / NX / stack canary | **線性記憶體內部的 C 記憶體錯誤可能更穩定可利用** | 無（用 ASan/UBSan 補） |
| 執行期反射 / 動態載入（核心） | 靜態連結導致重複打包 | dynamic linking（Emscripten 有自己的方案） |

---

## 七之二、部署層必檢

| 項目 | 要求 |
|---|---|
| MIME | `Content-Type: application/wasm`（否則 `instantiateStreaming` 直接拒絕） |
| 差分更新 | 壓縮字典傳輸（RFC 9842，`dcb`/`dcz`）：改版只傳增量（附錄 N §7-2） |
| ⚠️ 計時器精度 | **未跨來源隔離時 `performance.now()` 被降精度**——微秒級操作量不準（附錄 N §15） |
| CSP | `script-src 'wasm-unsafe-eval'`（Chrome 97+／FF 102+／Safari 16+） |
| 壓縮 | `Content-Encoding: br`（Wasm 壓縮率通常很好，常見 4 倍以上） |
| 完整性 | **`fetch()` 載入的 `.wasm` 沒有內建 SRI**；自行驗雜湊會犧牲串流編譯 |
| 程式碼快取 | 給 `.wasm` 穩定 URL（內容雜湊檔名最佳），回訪可跳過編譯 |
| 多 Worker | `WebAssembly.Module` 可結構化複製——**編譯一次 postMessage 給 N 個 Worker** |

## 八、安全紅線

| 紅線 | 為什麼 | 檢查方式 |
|---|---|---|
| **硬編碼金鑰** | 字串在 Data 段，**無法剝離** | `strings app.wasm \| grep -Ei 'sk-\|AKIA\|password\|secret'` |
| **核心商業機密演算法** | 逆向抬高的是成本不是可能性 | 問：對手明天拿到它，我還剩什麼？ |
| Export 段 | **規範強制，永遠可見** | `wasm-objdump -x app.wasm` |
| 「客戶端自我完整性校驗」 | 客戶端不可信，任何自證都能被本地改掉 | 把授權判斷放伺服器端 |

**逆向難度的六個層次**（見第 9 章）：讀字串（秒）→ 看 Export（秒）→ 反組譯 WAT（分）→ 偽碼還原（時）→ 看懂演算法（天–週）→ 還原可維護原始碼（月+，通常不划算）。

---

## 九、效能宣稱的閱讀守則

| 常見宣稱 | 真實情況 |
|---|---|
| 「Wasm 有 60–80% 原生效能」 | **視工作負載形狀而定**：純數值迴圈可達 90%+；手寫 AVX2 移植過來可能只有 40–50%；細碎跨界呼叫可能比 JS 還慢 |
| 「比 JS 快 10–30 倍」 | 只在特定基準成立。V8 充分預熱且型別穩定時，**JS 可逼近 Wasm，差距常在 1–2 倍內** |
| 「比 Docker 快 100 倍」 | **指冷啟動時可信**；穩態執行效能**低於**容器內的原生二進位 |
| 「檔案只有幾 KB 到幾 MB」 | 指純運算核心。OpenCV.js 6–9MB、solc 10–15MB、Pyodide 30–50MB |
| 「Wasm 記憶體安全」 | 指**沙盒邊界**安全（傷不到宿主），**不代表**你的 C 程式沒有記憶體錯誤 |
| 「Wasm 不可逆」 | **錯誤**。格式公開且結構化，反組譯比逆向 x86 容易得多 |
| 「開 Wasm GC 體積砍 80%」 | 只對「原本要打包語言執行期」的語言成立（Java/Kotlin/Dart）；**對 Rust/C/C++ 幾乎無效** |
| 「GC／memory64／尾呼叫是提案」 | **已過時**。這些在 **2025-09 的 Wasm 3.0** 就進了核心規範（見附錄 A） |
| 「用了 Wasm 所以是恆定時間」 | **錯誤**。恆定時間由原始碼寫法保證，Wasm 只是提供更可控的基底 |
