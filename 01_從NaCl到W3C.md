# 第一篇　起源與物理

# 第1章　從 NaCl 到 W3C：一個失敗的外掛，如何長成 Web 的第四種語言

> 二〇一一年，Google 在 Chrome 裡塞進一個叫 **Native Client** 的東西，宣稱可以在瀏覽器裡跑原生 C/C++。它技術上是成功的，商業上是徹底的失敗——因為除了 Chrome，沒有任何一家瀏覽器願意實作它。十四年後，同一件事被做成了，差別只有一個：**這次沒有人自己先動手。**

## 情境 1　那個技術上成功、政治上失敗的專案

**背景**：Web 的效能天花板不是新問題。JavaScript 是一門動態型別、垃圾回收、單執行緒的語言；它在 UI 邏輯上無可取代，但一旦碰上「密集計算」——影片轉碼、3D 渲染、影像濾波、大矩陣運算——它就撞牆。二〇一〇年前後，這個天花板下有兩條路線在互相競爭。

**第一條路：Google 的 NaCl / PNaCl（Native Client / Portable Native Client）**。它的思路直白到近乎粗暴：既然要快，那就**直接跑原生機器碼**。NaCl 用一套靜態驗證器（validator）掃描 x86 機器碼，強制指令對齊到 32 位元組邊界、禁止不安全的跳轉與系統呼叫，再把整段程式碼關進一個由分段暫存器（segment register）圍出來的沙盒裡。後來的 PNaCl 更進一步：不再分發 x86 機器碼，改分發 **LLVM bitcode**，由瀏覽器端做最後一哩的翻譯，藉此擺脫 CPU 架構綁定。

**它為什麼失敗**：不是因為它慢——它一點都不慢。它失敗於三件事：

- **它不是 Web**。NaCl 模組活在一個叫 `<embed>` 的外掛容器裡，跟 DOM、跟 JavaScript 之間隔著一層訊息傳遞介面。它是**寄生在網頁裡的原生程式**，而不是網頁的一部分。
- **它是一家公司的規格**。Mozilla 公開反對，Apple 與 Microsoft 從未表態要實作。一個只有 Chrome 支援的「Web 標準」，對開發者而言就是一個負債。
- **驗證器的複雜度過高**。要在每一種 CPU 架構上寫一套能證明其正確性的機器碼驗證器，是安全工程上極其昂貴的承諾。

**第二條路：Mozilla 的 asm.js**。它的思路完全相反，甚至有點狡猾——**根本不引入新東西**。asm.js 是 JavaScript 的一個**極度嚴格的子集**：所有數值運算都用位元運算標註型別（`x|0` 代表 32 位元整數，`+x` 代表雙精度浮點數），所有記憶體存取都走一個巨大的 `ArrayBuffer` 型別化陣列，禁止物件、禁止閉包、禁止垃圾回收。

```javascript
// asm.js 的典型長相：靠註解標記模組，靠位元運算把型別「寫死」在語法裡
function AsmModule(stdlib, foreign, heap) {
  "use asm";                          // ← 引擎看到這行，切換到 AOT 編譯路徑
  var HEAP32 = new stdlib.Int32Array(heap);
  function add(x, y) {
    x = x|0;                          // ← 宣告：x 是 32 位元整數
    y = y|0;
    return (x + y)|0;                 // ← 宣告：回傳值也是 32 位元整數
  }
  return { add: add };
}
```

**這一招的精髓在於：它是合法的 JavaScript。** 在不認識 asm.js 的瀏覽器裡，這段程式碼照樣正確執行（只是慢）；在認識它的瀏覽器裡，引擎看到 `"use asm"` 就跳過整套 JIT 預熱流程，直接把它當成靜態型別語言做 AOT（提前編譯）。**Emscripten** 把 C/C++ 經由 LLVM 編譯成這種 JavaScript，第一次讓「把整個 Unreal Engine 搬進瀏覽器」變成可以在會場上演示的事。

**兩條路各自證明了一件事**：NaCl 證明了**瀏覽器沙盒有能力安全地執行接近原生的程式碼**；asm.js 證明了**不需要外掛，光靠引擎優化就能拿到 2 倍以內的原生效能，而且四家瀏覽器都能實作**。

> 💡 君之一席話
> 與君一席話：**一個技術方案能否活下來，取決於它讓多少人「不需要同意」它。** NaCl 要求其他三家瀏覽器廠商全盤接受一套龐大的新執行環境與機器碼驗證器——那是要求別人下重注賭你的判斷；asm.js 只要求「你什麼都不用做，反正它是合法的 JS，只是我做了優化」——那是**把採納成本降到零，把優化收益留給願意跟進的人**。前者是政治提案，後者是技術事實。任何跨組織的標準之爭裡，這條規律都成立：**能在對方零成本的前提下先跑起來的方案，最後會贏；需要對方先付出代價才能開始的方案，通常連討論的機會都沒有。** 這也是為什麼 Wasm 最後選擇的路，是 asm.js 的政治智慧加上 NaCl 的技術野心。

> 🔍 進階點評──asm.js 的三個致命瓶頸，正是 Wasm 每一條設計的答案
> asm.js 贏得了政治，卻輸給了物理，而它輸的那三處，恰好逐條對應 Wasm 的設計動機。**第一，解析成本。** asm.js 再怎麼優化，它終究是**文字**。一個 40MB 的 asm.js 檔案，瀏覽器必須先做完整的 JavaScript 詞法與語法分析，才能發現「哦這是 asm.js」。純解析就吃掉幾百毫秒到數秒，而且這筆錢每次冷啟動都要付一遍。Wasm 的二進位格式把這一步壓縮到近乎為零——它可以**邊下載邊編譯**（streaming compilation），第一個位元組到達時編譯器就開工了。**第二，型別是靠慣例而非規範保證的。** `x|0` 是一個「引擎請你相信我」的約定；一旦程式碼稍有偏差，引擎會靜默地退回普通 JS 路徑，效能斷崖式下跌，而開發者往往看不到任何警告。Wasm 的型別是寫在二進位裡、由驗證器在載入時強制檢查的——**要嘛通過驗證、要嘛拒絕載入，沒有中間態。** 第三，**它永遠拿不到 JS 沒有的東西**。64 位元整數（`i64`）在 JavaScript 裡沒有原生型別，asm.js 只能用兩個 32 位元數模擬；SIMD 向量指令、原子操作、多執行緒共享記憶體，全都無法在 JS 語法裡表達。Wasm 一開始就把 `i32/i64/f32/f64` 四種數值型別寫進規範。所以真正的歷史敘事不是「Wasm 取代了 asm.js」，而是——**asm.js 是一次成功的市場驗證實驗，它證明了需求真實存在、證明了四家廠商的引擎都能吃下靜態型別的優化路徑，然後它把自己燒成了 Wasm 的燃料。** 這種「用一個註定被取代的方案，去證明下一個方案值得被投資」的策略，在基礎設施演進裡反覆出現。

## 情境 2　二〇一五：四家廠商為什麼突然坐到同一張桌子

**背景**：二〇一五年六月，Google、Mozilla、Microsoft、Apple 共同宣布開發一個全新的二進位格式標準——**WebAssembly**。這在瀏覽器戰爭的歷史上罕見到近乎反常：四家在幾乎所有議題上都互相掣肘的公司，在這件事上一次到位。

**為什麼這次能談成**，關鍵在於**三個「都不吃虧」**：

| 廠商 | 已投入的資產 | Wasm 保住了什麼 |
|---|---|---|
| Google | NaCl/PNaCl 的沙盒與驗證器經驗、V8 的 TurboFan 後端 | 沙盒技術有了標準化的出口，不必再獨自維護一個沒人跟進的規格 |
| Mozilla | asm.js、Emscripten 工具鏈、SpiderMonkey 的 OdinMonkey AOT 路徑 | 現有的 Emscripten 生態可以無痛換後端；asm.js 的優化投資不算白費 |
| Microsoft | Edge 需要快速追上效能敘事 | 拿到一個規格明確、實作邊界清楚的目標，不必逆向他人的 JS 引擎優化 |
| Apple | JavaScriptCore 的 FTL JIT | 一個「小而封閉」的規格，遠比「支援任意 LLVM bitcode」在安全審查上便宜 |

**設計上的關鍵取捨——它刻意做小**。Wasm MVP（最小可行產品，2017 年落地）刻意**不做**很多事：沒有垃圾回收、沒有例外處理、沒有多執行緒、不能碰 DOM、沒有系統呼叫、連字串型別都沒有。它只有四種數值型別、一塊連續記憶體、一組堆疊指令，以及一套能在載入時線性時間完成的驗證演算法。

**這種「刻意做小」是政治上的必要條件**：規格越小，四家獨立實作出現行為分歧的機率就越低；驗證器越簡單，安全審查的成本就越低；能力越少，被拿去做壞事的攻擊面就越窄。**Wasm 的 MVP 幾乎是一份「我們保證不會嚇到你」的協議書。**

**時間線**（可查證的規範里程碑）：

- **2015 年 6 月**：四方共同宣布 WebAssembly。
- **2017 年 3 月起**：Chrome、Firefox、Safari、Edge 陸續內建 Wasm 引擎，MVP 成為四大瀏覽器的共同能力。
- **2019 年 12 月**：W3C 正式將 WebAssembly Core Specification 1.0 定為推薦標準（Recommendation），Wasm 與 HTML、CSS、JavaScript 並列為 Web 的第四種核心語言。
- **2019 年起**：**WASI**（WebAssembly System Interface）提案面世，Wasm 開始脫離瀏覽器，進軍伺服器端、雲原生與邊緣計算。

> ⚠️ 真偽提醒
> 「四大巨頭 2015 年聯手」「2017 年四瀏覽器全面支援」「2019 年 W3C 定為標準」這三件事均為可查證的公開事實。但坊間常見的加料版本——例如把 Wasm 說成「W3C 欽定的第四種程式語言，將取代 JavaScript」——是敘事誇張。**規範文件從未把 Wasm 定位為 JavaScript 的替代品，而是明確定位為互補（complement）**：Wasm 至今無法獨立操作 DOM、無法獨立加載自己，它的每一次啟動都需要 JavaScript（或宿主環境）遞給它第一把鑰匙。

> 🔍 進階點評──「刻意做小」的複利，與它現在正在付的利息
> MVP 的極簡主義在 2017 年是勝利的關鍵，但到了 2026 年，它的帳單也全部到期了。**帳單一：沒有 GC，等於把整個 Java/Kotlin/Dart/C# 生態擋在門外。** 這些語言必須把自己的垃圾回收器整包編譯進 `.wasm`，導致產出動輒數 MB 起跳——這正是後來 **Wasm GC**（讓 Wasm 直接複用宿主引擎既有的 GC，**已於 Wasm 3.0 進入核心規範**）的直接動機，而 Wasm GC 落地後，Kotlin/Wasm、Dart（Flutter Web）這類語言的產出體積能大幅縮減。**帳單二：沒有例外處理，等於 C++ 的 `try/catch` 只能靠 JavaScript 蹦床（trampoline）模擬**，每一次跨邊界都是可觀的開銷——這是 **原生例外處理**（Wasm 3.0）的動機。**帳單三：沒有尾呼叫，等於函數式語言（Scheme、OCaml、Haskell）的深層遞迴一定爆棧**——這是 **尾呼叫**（`return_call`，Wasm 3.0）的動機。**帳單四：只有數值型別，等於每一次字串往返都要手動編碼解碼**——這是 **Reference Types**、**JS String Builtins** 一路演化的動機。所以看待 Wasm 提案清單（SIMD、threads、bulk memory、multi-value、reference types、GC、exception handling、tail call、memory64、multiple memories、relaxed SIMD、branch hinting……）的正確方式不是「功能越來越多」，而是——**每一條提案都在償還 2015 年為了讓四家坐上談判桌而刻意欠下的一筆債。**
> **而這筆債，到 2025 年 9 月基本上還完了。** 那個月 **WebAssembly 3.0 宣布完成**，一次把 **GC、Memory64、例外處理、尾呼叫、多重記憶體、具型別的函數參考、擴充常數運算式、分支提示、128 位元 SIMD** 全部收進核心規範。**十年前為了達成共識砍掉的東西，用十年逐條加了回來，而核心模型一次都沒有崩。**
>
> 這條規律值得所有做平台的人記住：**為了拿到共識而做的減法，會在你成功之後，變成你必須逐條加回來的技術債；而那筆債能不能還得起，取決於你當初砍掉的東西，有沒有破壞核心模型的一致性。** Wasm 還得起，是因為它砍的是「能力」，沒有砍「型別安全」與「線性記憶體」這兩根柱子。**如果 2015 年他們為了效能而在型別系統上讓步，這十年就不會是還債，會是重寫。**

## 情境 3　破圈：WASI，以及那句被反覆引用的話

**背景**：二〇一九年，Wasm 走出了瀏覽器。核心規範裡的 Wasm 模組**本來就沒有任何系統能力**——它不能開檔案、不能建立連線、不能讀時鐘。所有能力都必須由宿主環境（host）以 **import** 的形式明確遞進去。這個設計原本是為了瀏覽器沙盒，卻意外地成了伺服器端最想要的東西。

**WASI 做了什麼**：它定義了一套**與作業系統無關的標準介面**——檔案、時鐘、隨機數、環境變數、網路（在後續版本中）——讓同一份 `.wasm` 可以在 Linux、Windows、macOS 上被同一套語意執行。更關鍵的是它的安全模型：

- **能力式安全（capability-based security）**：Wasm 模組預設「兩手空空」。它沒有 `open()` 的權限，只有宿主**預先開啟並遞給它**的目錄控制代碼（pre-opened directories）。
- **路徑逃逸在執行期被攔截**：模組若試圖存取 `/sandbox/../etc/passwd`，會被執行期在能力邊界上直接擋下，而不是靠檔案系統權限碰運氣。

```bash
# Wasmtime：把主機的 ./my_storage 映射為 Wasm 眼中的 /sandbox，且只有這一個目錄
wasmtime run --dir=./my_storage::/sandbox my_server.wasm
```

**這跟傳統伺服器程序的差別是質變，不是量變**。一個被攻破的 Node.js 程序，拿到的是**執行它的那個使用者的全部權限**；一個被攻破的 Wasm 模組，拿到的是**宿主明確遞進去的那幾樣東西，一樣都不會多**。這是「預設拒絕」與「預設允許」的差異。

**那句被反覆引用的話**：Docker 創辦人 Solomon Hykes 在 2019 年那則廣為流傳的推文裡說——**「如果 2008 年就有 WASM+WASI，我們根本不需要發明 Docker。」**

> ⚠️ 真偽提醒
> 這句話確實存在，且經常被引用；但它常被斷章取義成「Wasm 將取代 Docker」。**原推文的下一句是「Wasm 是伺服器運算的未來」而非「容器已死」**，而 Hykes 本人也在後續討論中澄清這是對「輕量、可攜、跨平台」這一組性質的稱讚，不是宣告容器過時。現實裡兩者是**互補**的：Docker/OCI 隔離的是整個作業系統使用者空間，能跑任何既有的二進位；Wasm 只隔離單一應用，換來啟動時間與體積的量級優勢，但**只能跑「重新編譯過」的東西**。實務上最常見的部署形態是 **Wasm 模組跑在容器化的執行期裡**（如 Kubernetes 上的 runwasi / Krustlet 一類方案），而不是二選一。

**Wasm 在後端的三個具體優勢**（可量化、可驗證）：

| 面向 | 容器（OCI / Docker） | Wasm + WASI |
|---|---|---|
| 冷啟動 | 數十毫秒 ~ 數秒（拉映像、起 namespace、init 程序） | 微秒 ~ 數毫秒（模組實例化，權重可預先編譯快取） |
| 產物體積 | 數十 MB ~ 數百 MB（含基礎映像與作業系統層） | 數十 KB ~ 數 MB（只有你的程式碼與必要 runtime） |
| 隔離邊界 | Linux namespace + cgroup + seccomp（作業系統層） | 記憶體安全的虛擬機（語言層），加上能力式 API 授權 |
| 跨架構 | 需為每個 CPU 架構各出一版映像 | 單一 `.wasm` 到處跑（由執行期做最後一哩編譯） |
| 能跑什麼 | 任何既有 Linux 二進位 | **只有重新編譯過的東西**（這是最大的現實限制） |

**主要執行期**：**Wasmtime**（Bytecode Alliance 主導，WASI 的參考實作與規範推進者）、**WasmEdge**（CNCF 沙盒專案，針對雲原生、微服務與 AI 推理優化，支援在 Wasm 內調用 GPU）、**Wasmer**（強調可攜與跨語言嵌入）、**Spin**（Fermyon 出品，用來建構與執行 Wasm 微服務的框架，讓開發者用熟悉的語言寫 Serverless Function）。

> 💡 君之一席話
> 與君一席話：**能力式安全的真正價值，不在於它更安全，而在於它讓「安全」變成一件可以在部署腳本裡讀懂的事。** 傳統的沙盒安全是**否定式**的——你要靠 seccomp 白名單、AppArmor 規則、capability drop 去一條一條剝奪權限，而你永遠不確定自己有沒有漏掉一條。能力式安全是**肯定式**的——那行 `--dir=./my_storage::/sandbox` 就是這個服務所擁有能力的**完整清單**，多一個字都沒有。安全審查從「證明沒有漏洞」變成「讀懂一行參數」，這是審計成本的量級下降。把這條原理放大，你會在資料庫權限、雲端 IAM、OAuth scope、瀏覽器擴充套件權限裡到處看見同一個掙扎：**所有預設全給、事後扣除的權限模型，最後都會演化成預設全無、逐項授予；而每一次這種遷移，都貴到讓人拖延十年。**

## 情境 4　它在哪裡真的落地了：四個不可辯駁的商業案例

**背景**：技術敘事再漂亮，也不如四個「你天天在用」的產品有說服力。以下四個是 Wasm 最常被引用、且有公開技術文章佐證的重量級落地：

- **Figma**：核心渲染引擎由 C++ 編寫並編譯為 Wasm。這是網頁端能流暢編輯數萬個向量圖層的關鍵。Figma 公開的工程文章記載了從 asm.js 遷移到 Wasm 後**載入時間顯著縮短**的實測。
- **Adobe Photoshop Web**：Adobe 把數百萬行的 C++ 歷史程式碼透過 Wasm（配合 Emscripten 與後續的多執行緒、SIMD 支援）移植到瀏覽器，讓使用者無須安裝就能做專業圖層編輯。
- **Google Earth**：早期的 3D 地球需要專屬外掛（NPAPI 時代的產物），如今透過 Wasm 在瀏覽器內直接渲染全球 3D 地形。
- **AutoCAD Web**：把累積數十年的 C++ CAD 核心搬上瀏覽器，工程圖檔不必再繞路桌面軟體。

**這四個案例的共同點，比它們的差異更重要**：它們全都是**「已經有數百萬行成熟 C/C++ 資產」的公司**。Wasm 對它們的價值不是「跑得快」，而是**「不用重寫」**——把二十年的幾何核心、影像演算法、字型排版引擎，用一條編譯管線搬到一個全新的分發通道上，而不是叫三百個工程師用 TypeScript 重寫一遍。

**反過來說，這也劃出了 Wasm 不划算的邊界**：如果你要做的是一個表單 CRUD、一個部落格、一個電商前台，**Wasm 對你毫無價值**，甚至是純負債（多一層編譯、多一份體積、多一套除錯困難）。這件事在第 3 章會用限制清單講得更狠。

> 💡 君之一席話
> 與君一席話：**一項新技術真正的商業價值，往往不在「它能做什麼新的事」，而在「它讓多少既有的資產免於報廢」。** Figma、Adobe、Autodesk 這幾家採用 Wasm，圖的從來不是效能報表上那幾個百分點，而是**幾十年 C++ 資產的殘值被一次性盤活**——那是一筆帳面上看不見、卻大到足以決定一家公司要不要押注新平台的數字。這就是為什麼「顛覆式創新」的敘事常常誤導人：**真正推動大型組織採用新技術的，往往是保守的動機（保住既有投資），而不是激進的動機（擁抱未來）。** 當你要說服一個組織採用某項技術時，講「它能讓你做到以前做不到的事」通常打動不了決策者；講「它讓你已經付過的那筆錢，在新的通道上再賺一次」才會。

## 本章小結

- Wasm 的前史是兩條路線的合流：**NaCl 證明了瀏覽器沙盒能安全跑接近原生的程式碼，asm.js 證明了不需要外掛就能拿到接近原生的效能且四家都能實作**——前者貢獻技術野心，後者貢獻政治智慧。
- **2015 年四方共識、2017 年四瀏覽器落地、2019 年 W3C 定為標準**（皆可查證）。談成的關鍵是「刻意做小」：沒有 GC、沒有例外、沒有執行緒、不能碰 DOM——**規格越小，四家獨立實作出現分歧的機率越低。**
- 今天的每一條 Wasm 特性（SIMD、threads、GC、exception handling、tail call、memory64、multiple memories……），本質上都是在**償還 2015 年為了達成共識而刻意欠下的債**——而這筆債在 **2025 年 9 月的 WebAssembly 3.0** 基本還清。
- **WASI 讓 Wasm 破圈**，其核心不是「能開檔案了」，而是**能力式安全**：模組預設兩手空空，所有能力必須由宿主明確遞入，且完整清單就寫在啟動參數裡。
- 「如果 2008 年有 WASI 就不需要發明 Docker」確有其言，但**不等於容器已死**——實務上最常見的是 Wasm 跑在容器化的執行期裡（真偽待考處見情境 3 ⚠️）。
- Figma、Photoshop Web、Google Earth、AutoCAD Web 的共同點是：它們都握有數百萬行成熟 C++ 資產。**Wasm 對它們的價值是「不用重寫」，而不是「跑得快」。**

歷史講完了，該看機器本身。下一個問題很具體：**當瀏覽器拿到那串以 `\0asm` 開頭的位元組，它到底做了什麼？為什麼那串位元組可以被證明是安全的？** 翻到第 2 章。
